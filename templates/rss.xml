<rss version="2.0">
    <channel>
        <title>Tris's Blog</title>
        <link>https://tris.fyi/blog/</link>
        <description>Tris Emmy Wilson's lab notebook, random thoughts, and writeups of other projects.</description>
        <language>en-US</language>
        <docs>https://www.rssboard.org/rss-specification</docs>
        <generator>Tris's Website Generator</generator>
        <webMaster>blog@tris.fyi (Tris Emmy Wilson)</webMaster>

        {% for post in posts if post.published|default(true) and (not post.hidden|default(false)) %}
        <item>
            <title>{{ post.title }}{% if post.subtitle %}: {{ post.subtitle }}{% endif %}</title>
            <link>{{ url_for("post", slug=post.slug, _external=True, _scheme="https") }}</link>
            {% if post.summary %}<description>{{ post.summary }}</description>{% endif %}
            <pubDate>{{ post.posted_on.strftime("%a, %d %b %Y %T GMT") }}</pubDate>
        </item>
        {% endfor %}
    </channel>
</rss>
